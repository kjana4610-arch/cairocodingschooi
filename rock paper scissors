
#include <iostream>

#include <cstdlib>
#include <ctime>
using namespace std;

class Game
{
private:
    int player;
    int computer;

public:
    Game()
    {
        player = 0;
        computer = 0;
    }

    void getPlayerChoice()
    {
        cout << "Choose:\n";
        cout << "1. Rock\n";
        cout << "2. Paper\n";
        cout << "3. Scissors\n";
        cin >> player;
    }

    void getComputerChoice()
    {
        computer = rand() % 3 + 1;
    }

    void showChoice(int choice)
    {
        if (choice == 1)
            cout << "Rock";
        else if (choice == 2)
            cout << "Paper";
        else
            cout << "Scissors";
    }

    void findWinner()
    {
        cout << "\nYou chose: ";
        showChoice(player);

        cout << "\nComputer chose: ";
        showChoice(computer);

        cout << "\n\n";#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int x;
    cin >> x;

    int mn = x;
    int pos = 1;

    for (int i = 2; i <= n; i++)
    {
        cin >> x;

        if (x < mn)
        {
            mn = x;
            pos = i;
        }
    }

    cout << mn << " " << pos;

    return 0;
}

        if (player == computer)
        {
            cout << "Draw!\n";
        }
        else if ((player == 1 && computer == 3) ||
                 (player == 2 && computer == 1) ||
                 (player == 3 && computer == 2))
        {
            cout << "You Win!\n";
        }
        else
        {
            cout << "Computer Wins!\n";
        }
    }

    void startGame()
    {
        getPlayerChoice();
        getComputerChoice();
        findWinner();
    }
};

int main()
{
    srand(time(0));

    Game game;
    game.startGame();

    return 0;
}
